'use strict';

const expect = require('../../helpers/expect');

describe('`ember electron:make` command', () => {
  describe('#builder', () => {
    it('calls forge `package` api with passed options');
  });
});
